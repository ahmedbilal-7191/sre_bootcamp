apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-state-metrics-dashboard
  labels:
    grafana_dashboard: "dynamic"
data:
  kube-state-metrics-dashboard.json: |
{{ .Files.Get "dashboards/kube-state.json" | indent 4 }}