apiVersion: v1
kind: Secret
metadata:
  name: private-repo-creds
  labels:
    # This label is crucial for Argo CD to auto-discover and use the secret
    argocd.argoproj.io/secret-type: repository
  namespace: argocd # Assuming Argo CD is installed in the 'argocd' namespace
type: Opaque
stringData: # Use stringData for convenience, kubectl will base64 encode it on apply
  # The full HTTPS URL of your Git repository
  url: "https://github.com/ahmedbilal-7191/sre_bootcamp.git"
  # Authentication method: 'basic' for username/password
  type: git
  # Your Git username (e.g., your GitHub/GitLab username or a Personal Access Token username)
  username: "ahmedbilal-7191"
  # Your Git password or a Personal Access Token (PAT)
  password: "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"
  # Optional: For better security, you might want to specify the host
  # name: "github.com"
