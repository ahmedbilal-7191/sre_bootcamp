Flask==2.3.3
Flask-SQLAlchemy==3.1.1 #ORM
Flask-Migrate==4.0.5 #DB migration
psycopg2-binary==2.9.9 #Postgresql Driver
python-dotenv==1.0.0 #Load env variables from .env file
gunicorn==23.0.0 #WSGI server
marshmallow==3.21.1 #Serialization/Deserialization validation too
marshmallow-sqlalchemy==1.4.1
flask-marshmallow==0.15.0
prometheus-client>=0.20.0
python-json-logger
# Since testing dependencies should not be included in the main requirements file, they have been moved to a separate development requirements file.
pytest==8.3.2
pytest-flask==1.3.0
pytest-cov
pylint
flake8
# check the normal k8s deployment first one is the setup the vault then the eso then the backedn+DB
# next level is the vault eso then the helm based argocd setup one click
# next level is the vault eso then log&mon alerts,svc,monitoring which has secrets backendapp,grafana,postgresqlexporter,alertmanger secerts,argocdnormal git repo secertes
# top priority application svc monitoring and the alert of it then goes the normal secerets and restart alerts  